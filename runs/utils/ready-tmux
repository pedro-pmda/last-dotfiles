#!/usr/bin/env bash
# run-os: any
# run-desc: 🧩 Ejecutar el hook .ready-tmux del proyecto

caller_dir="$(pwd -P)"
ready_tmux="$caller_dir/.ready-tmux"

if [[ -x "$ready_tmux" ]]; then
    "$ready_tmux"
elif [[ -x "$HOME/.ready-tmux" ]]; then
    "$HOME/.ready-tmux"
fi

